import SwiftUI

struct AuthenticationView: View {
    var body: some View {
        NavigationStack {
            Text("Authentication")
                .navigationTitle("Authentication")
        }
    }
}

#Preview {
    AuthenticationView()
}
